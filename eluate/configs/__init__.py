# SPDX-License-Identifier: MIT
"""Bundled model configuration files.

Located inside the package so they're shipped via ``package-data`` and
reachable from a wheel install via ``importlib.resources``.
"""
